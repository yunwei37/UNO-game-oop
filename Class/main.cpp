#include <iostream>
#include <vector>
#inlcude < stack>
#include <string>

#include "UNOConstants.h"
#include "GameConstants.h"
#include "Server.h"

using namespace std;

int main()
{
	int mode;
	Server myserver;
	cout << "UNO Card V1.0" << endl;

	//only code logic for run in CMD!!!

	while (!gameover)
	{
		for (p in player)
		{
			if(p's turn)
			{
				if (p == PC)
					playPC();
				else
					prin(palyer ID)
						prin(lasted played card)
							prin(all your card)
								prin(choose
									 : draw or play a card) if (draw)
					{
						requestcard();
					}
				if (play)
					playthiscard(card ID);
			}
		}
	}
	prin(the score);
}